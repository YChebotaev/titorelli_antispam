{
  "name": "@titorelli_antispam/telegram-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon",
    "migrate": "knex --knexfile knexfile.ts migrate:latest",
    "seed": "knex --knexfile knexfile.ts seed:run",
    "rollback": "knex --knexfile knexfile.ts migrate:rollback",
    "make:seed": "knex seed:make $2 --knexfile knexfile.ts -x ts",
    "make:migration": "knex migrate:make $2 --knexfile knexfile.ts -x ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "fs-extra": "^10.1.0",
    "nodemon": "^2.0.20",
    "telegraf": "^4.10.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "vitest": "^0.25.2"
  }
}
